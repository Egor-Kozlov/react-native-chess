import { StyleSheet } from "react-native";

export default StyleSheet.create({
  board: {
    borderWidth: 7,
    borderColor: "#dedede",
    width: "100%",
    flexWrap: "wrap-reverse",
    flexDirection: "row",
  },
});
